/*import { addDoc, collection } from "firebase/firestore"
import { bd } from "../../firebase/firebaseConfig"

export const addDate = ( cita ) => {
    return (dispatch) => {
        addDoc(collection(bd, "citas"), cita)
        .then( (resp) => {
            //dispatch( addDateSync( cita ) )
            alert('cita agendada con exito')
        })
    }
}*/